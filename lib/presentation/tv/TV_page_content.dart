import 'package:flutter/cupertino.dart';

class TVPageContent extends StatelessWidget {
  const TVPageContent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Drugi ekran'),
    );
  }
}
