import 'package:flutter/material.dart';

class WishlistPageContent extends StatelessWidget {
  const WishlistPageContent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Trzeci ekran'),
    );
  }
}
