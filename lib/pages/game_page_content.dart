import 'package:flutter/cupertino.dart';

class GamePageContent extends StatelessWidget {
  const GamePageContent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Piąty ekran'),
    );
  }
}
